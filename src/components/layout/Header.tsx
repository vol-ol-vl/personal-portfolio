const Header = () => {
  const name = 'Olga Volkova';
  const role = 'Frontend Developer';

  return (
    <header>
        <h1>{name}</h1>
        <p>{role}</p>
    </header>
  );
};

export default Header;